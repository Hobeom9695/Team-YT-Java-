<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.study.springboot.dao.ItUserDao">

	<select id="userSelect" resultType="com.study.springboot.dto.UserDto">
		select nickname from user_yt where id = #{0}
	</select>

	<select id="checkid" parameterType="java.util.HashMap" resultType="_int">
		select count(id) from user_yt where id = #{item1}
	</select>
	
	<select id="checkNick" parameterType="java.util.HashMap" resultType="_int">
		select count(nickname) from user_yt where nickname = #{item1}
	</select>
	
	<select id="checkSns" parameterType="java.util.HashMap" resultType="_int">
		select count(sns_id) from sns_info where sns_id = #{item1}
	</select>
	
	<select id="userViewDao" resultType="com.study.springboot.dto.UserDto">
		select * from simple_bbs where id = #{0}
	</select>
	
	<insert id="join" parameterType="java.util.HashMap">
		insert into user_yt (user_num, id, password, user_name,
                     		 user_telnum, nickname, email, company, authority, enabled)
			values (user_yt_seq.nextval, #{item1}, #{item2}, #{item3},
					#{item4}, #{item5}, #{item6}, #{item7}, 'ROLE_NORMAL', 1)
	</insert>
	
	<insert id="signUpSns" parameterType="java.util.HashMap">
		insert into user_yt (user_num, id, sns_type, user_name, email, authority, enabled)
			values (user_yt_seq.nextval, #{item1}, 'NAVER', #{item2}, #{item3},
					'ROLE_NORMAL', 1)
	</insert>
	
	<delete id="userDeleteDao">
		delete from simple_bbs where id = #{_id}
	</delete>
	
	<select id="articleCount" resultType="_int">
		select count(*) from simple_bbs
	</select>
	
</mapper>